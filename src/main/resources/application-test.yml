spring:
  datasource:
    url: jdbc:mysql://localhost:3307/bankdb-integration-test?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC
    username: bankuser
    password: nonmelaricordo
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
  liquibase:
    change-log: classpath:db-integration-test/changelog/db.changelog-master.xml
