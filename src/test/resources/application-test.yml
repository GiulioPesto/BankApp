spring:
  datasource:
    # Configurazione Supabase per i test di integrazione
    url: jdbc:postgresql://aws-1-eu-west-3.pooler.supabase.com:5432/postgres
    username: postgres.ehgsmaiqpbtpjiiqkpdx
    password: PestoPazzo3011!
    driver-class-name: org.postgresql.Driver
    hikari:
      # Configurazione per test - ridotto al minimo per evitare MaxClientsInSessionMode
      maximum-pool-size: 2
      minimum-idle: 1
      connection-timeout: 30000
      idle-timeout: 300000
      max-lifetime: 600000
  jpa:
    hibernate:
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
      ddl-auto: create-drop
    properties:
      hibernate:
        default_schema: bank_schema_test
  liquibase:
    enabled: false
